/**
 * The default resolution of `@/design/entry`, for TypeScript and editors.
 *
 * At build time the alias in next.config.ts replaces this with the depth
 * design's entry — `entry.depth.tsx` — so this file is never the one that ships.
 */
export { DesignHome } from "./entry.depth";
