/**
 * The default resolution of `@/design/fonts`, for TypeScript and editors. At
 * build time the alias in next.config.ts swaps in the faces for the design
 * being built.
 */
export { fontClass } from "./fonts.stack";
